# Copy this file to .env and fill values
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_db_user
DB_PASS=your_db_password
JWT_SECRET=some_long_random_secret
PORT=4000

# Optional: set this to a parent domain (e.g. .onrender.com) to allow the auth cookie
# to be shared across subdomains (frontend and backend). Only used in production.
# COOKIE_DOMAIN=.onrender.com
